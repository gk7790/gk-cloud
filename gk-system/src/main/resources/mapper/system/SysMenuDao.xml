<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gk.system.dao.SysMenuDao">

    <resultMap id="SysMenuResultMap" type="com.gk.system.entity.SysMenuEntity">
        <id property="id" column="id"/>
        <result property="pid" column="pid"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="name" column="name"/>
        <result property="path" column="path"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="activePath" column="active_path"/>
        <result property="authCode" column="auth_code"/>
        <result property="component" column="component"/>
        <result property="sort" column="sort"/>
        <!-- 关键：meta 字段加 typeHandler -->
        <result property="meta" column="meta" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
    </resultMap>

	<select id="getById" resultType="com.gk.system.entity.SysMenuEntity">
		select t1.*,
		(select lang.field_value from sys_language lang where lang.table_name='sys_menu' and lang.field_name='name'
		and lang.table_id=t1.pid and lang.language=#{language}) as parentName,
		(select lang.field_value from sys_language lang where lang.table_name='sys_menu' and lang.field_name='name'
		and lang.table_id=t1.id and lang.language=#{language}) as name
		from sys_menu t1
		where t1.id = #{id}
	</select>

	<select id="getMenuList" resultMap="SysMenuResultMap">
		select t1.*
	  	from sys_menu t1
		<where>
			<if test="type != null">
				t1.type = #{type}
			</if>
		</where>
		order by t1.sort asc
	</select>

	<select id="getUserMenuList" resultType="com.gk.system.entity.SysMenuEntity">
		select t3.*, (select lang.field_value from sys_language lang where lang.table_name='sys_menu' and lang.field_name='name'
		and lang.table_id=t3.id and lang.language=#{language}) as name from sys_role_user t1
		left join sys_role_menu t2 on t1.role_id = t2.role_id
		left join sys_menu t3 on t2.menu_id = t3.id
		where t1.user_id = #{userId}
		<if test="menuType != null">
			and t3.menu_type = #{menuType}
		</if>
		order by t3.sort asc
	</select>

	<select id="getUserPermissionsList" resultType="string">
		select t3.auth_code from sys_role_user t1 left join sys_role_menu t2 on t1.role_id = t2.role_id
			left join sys_menu t3 on t2.menu_id = t3.id
		where t1.user_id = #{userId} order by t3.sort asc
	</select>

	<select id="getPermissionsList" resultType="string">
		select auth_code from sys_menu
	</select>

	<select id="getListPid" resultType="com.gk.system.entity.SysMenuEntity">
		select * from sys_menu where pid = #{value}
	</select>
	
</mapper>